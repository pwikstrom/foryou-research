# Backup creation skipped to avoid clutter, will use version control if needed.
# Actually I'll trust the tool undo capabilities if something goes wrong.
# But I will append the new function to data_service.py now.
